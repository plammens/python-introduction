# -- None (special value indicating the absence of value) --
None  # this is how you write None
print(None)

var = None
# `var` is now a valid variable, but it doesn't contain any "meaningful" object
print(var)
print(var is None)
